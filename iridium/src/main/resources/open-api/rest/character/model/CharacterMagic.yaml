type: object
properties:
  id:
    type: string
    format: uuid
  name:
    type: string
  spells:
    type: array
    items:
      $ref: 'CharacterSpell.yaml'
    uniqueItems: true
    description: the spells that the character knows how to use