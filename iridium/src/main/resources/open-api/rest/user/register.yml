post:
  tags:
    - authorization
  description: register user
  operationId: register
  requestBody:
    description: user data to register
    content:
      application/json:
        schema:
          $ref: 'model/RegisterRequest.yaml'
    required: true
  responses:
    '201':
      description: created
      content:
        application/json:
          schema:
            type: string