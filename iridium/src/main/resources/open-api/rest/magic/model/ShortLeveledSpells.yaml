type: object
required:
  - level
  - spells
properties:
  level:
    type: integer
    desctiprion: the magic level for the spells
    maxValue: 5
    minValue: 1
    example: 4
  spells:
    type: array
    items:
      $ref: 'ShortSpellResponse.yaml'
    uniqueItems: true