type: object
required:
  - level
  - spells
properties:
  level:
    type: integer
    description: the magic level for the spells
    example: 4
  spells:
    type: array
    items:
      $ref: 'SpellResponse.yaml'
    uniqueItems: true